server:
  config:
    activate:
      on-profile: local-mysql
  address: localhost
  port: 8080
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: nahyun919ixs
    url: jdbc:mysql://localhost:3306/MiliWili?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
  jpa:
    hibernate:
      ddl-auto: create-drop
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    generate-ddl: false
    show-sql: true
#  quartz:
#    scheduler-name: QuartzScheduler
#    properties:
#      org:
#        quartz:
#          threadPool:
#            threadCount: 5